# =============================================================================
# AI Safety SaaS 환경 설정
# =============================================================================
# 이 파일을 .env.local로 복사한 후 실제 값으로 변경하세요.
#
# 주의사항:
# - .env.local 파일은 절대 Git에 커밋하지 마세요
# - 민감한 정보는 반드시 안전하게 보관하세요
# - 프로덕션 환경에서는 다른 값을 사용하세요
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase 데이터베이스 설정
# -----------------------------------------------------------------------------
# Supabase 프로젝트 설정에서 가져올 수 있습니다:
# https://app.supabase.com/project/YOUR_PROJECT_ID/settings/api

# Supabase 프로젝트 URL (예: https://xxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url

# Supabase 익명 키 (공개 키)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase 서비스 역할 키 (서버사이드 전용, 매우 민감!)
# 주의: 이 키는 절대 클라이언트 사이드에서 사용하지 마세요
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# -----------------------------------------------------------------------------
# AI 서비스 설정
# -----------------------------------------------------------------------------
# OpenAI API 키 (문서 자동 생성에 사용)
OPENAI_API_KEY=your-openai-api-key

# OpenAI 모델 설정 (선택사항)
OPENAI_MODEL=gpt-4-turbo-preview

# -----------------------------------------------------------------------------
# 애플리케이션 설정
# -----------------------------------------------------------------------------
# 애플리케이션 기본 URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 개발/프로덕션 환경 구분
NODE_ENV=development

# 애플리케이션 이름
NEXT_PUBLIC_APP_NAME=AI Safety SaaS

# -----------------------------------------------------------------------------
# 파일 업로드 설정
# -----------------------------------------------------------------------------
# 최대 파일 크기 (바이트 단위, 기본: 10MB)
MAX_FILE_SIZE=10485760

# 허용된 파일 형식 (쉼표로 구분)
ALLOWED_FILE_TYPES=pdf,doc,docx,xls,xlsx,txt,jpg,jpeg,png

# 파일 저장 경로 (Supabase Storage 버킷 이름)
SUPABASE_STORAGE_BUCKET=documents

# -----------------------------------------------------------------------------
# 보안 설정
# -----------------------------------------------------------------------------
# JWT 비밀 키 (세션 관리용)
NEXTAUTH_SECRET=your-nextauth-secret

# 애플리케이션 도메인
NEXTAUTH_URL=http://localhost:3000

# CORS 허용 도메인 (프로덕션에서 필수)
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# -----------------------------------------------------------------------------
# 이메일 설정 (알림 발송용)
# -----------------------------------------------------------------------------
# SMTP 서버 설정
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# 발신자 이메일
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=AI Safety SaaS

# -----------------------------------------------------------------------------
# 로깅 및 모니터링
# -----------------------------------------------------------------------------
# 로그 레벨 (debug, info, warn, error)
LOG_LEVEL=info

# Sentry DSN (에러 추적, 선택사항)
SENTRY_DSN=your-sentry-dsn

# -----------------------------------------------------------------------------
# 데이터베이스 백업 설정
# -----------------------------------------------------------------------------
# 자동 백업 활성화 (true/false)
AUTO_BACKUP_ENABLED=true

# 백업 주기 (일 단위)
BACKUP_INTERVAL_DAYS=7

# -----------------------------------------------------------------------------
# 캐시 설정
# -----------------------------------------------------------------------------
# Redis URL (캐시 서버, 선택사항)
REDIS_URL=redis://localhost:6379

# 캐시 TTL (초 단위, 기본: 1시간)
CACHE_TTL=3600